# The service is activated by the Goodix input device through SYSTEMD_WANTS.
# It must remain static so it is not started when the Halo surface is absent.
halo-keyboard: systemd-service-file-missing-install-key [usr/lib/systemd/system/halo-keyboard.service]

# This is a new upstream GitHub project, not an upload closing a Debian bug.
halo-keyboard: initial-upload-closes-no-bugs [usr/share/doc/halo-keyboard/changelog.Debian.gz:1]
